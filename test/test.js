'use strict';
var assert = require('assert');
var octoberBackend = require('../');

describe('october-backend node module', function () {
  it('must have at least one test', function () {
    assert(true, 'I was too lazy to write any tests. Shame on me.');
  });
});
