/*
import Recents from '../../Collections/Recents';

//{user_id : 'testing123'}
var d = new Date();
var rec = new Recents(null, null, 'testing12');
var inputObj = {
	userId : 'testing12',
	time_accessed : d.getTime(),
	result : {
		_id : 'uniqueId123',
		stuff : 'stuff'
	}
};

//rec.addRecent(inputObj).then((d) => console.log(d));

rec.getRecents().then((res) => console.log(res));
*/
