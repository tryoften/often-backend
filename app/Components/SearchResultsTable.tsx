import * as React from 'react';
import Element = JSX.Element;

export default class SearchResultsTable extends React.Component<any, any> {
	render(): Element {
		return (
			<div className="search-results"></div>
		);
	}
}
